<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <PackageId>FrayNet.Xunit</PackageId>
    <Description>xUnit integration for FrayNet: the [FrayFact] attribute runs a test body under controlled concurrency exploration and fails with the schedule that broke it.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit.extensibility.execution" Version="2.5.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Fray\Fray.csproj" />
  </ItemGroup>

</Project>
